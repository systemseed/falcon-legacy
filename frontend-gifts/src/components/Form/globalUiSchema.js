// Global default uiSchema.
// Allows to override ui widgets and ui settings of the form.
// This will be used by default unless you pass in a uiSchema prop.
// Don't directly import this file, use named import from the
// Form component.
// See https://github.com/mozilla-services/react-jsonschema-form#the-uischema-object
const uiSchema = {
};

export default uiSchema;
