<?php

class IrelandEndpointCest
{
  /**
   * Test fake IE api endpoint.
   *
   * @param \ApiTester $I
   */
    public function getIeEndpoint(ApiTester $I) {
      /*$I->amGoingTo('Send GET request to /ie?_format=json as anonymous user.');
      $I->sendGET('/ie', ['_format' => 'json']);

      $I->expectTo('See JSON response with 200 OK code.');
      $I->seeResponseCodeIs(\Codeception\Util\HttpCode::OK);
      $I->seeResponseIsJson();

      $I->expectTo('See valid IE data');
      $I->seeResponseContainsJson([
        "ie" => TRUE,
      ]);*/
    }
}
